import { title } from "@/components/primitives";

export default function MembersPage() {
    return (
        <div>
            <h1 className={title({ color: 'blue' })}>Strefa członka</h1>
        </div>
    );
}
