
export type SectionView = {
    id: string;
    inMenu: boolean;
    slug: string;
    published: boolean;
};


export type ArticleView = {
    id: string;
    inMenu: boolean;
    slug: string;
    published: boolean;
};